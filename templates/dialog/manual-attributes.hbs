<div>
    <table>
        <thead></thead>
        <tbody>
    {{#each abilities as |ability key|}}
        {{#unless ability.skip}}
            <tr>
                <th>{{key}}</th>
                <td><label>
                    <input class="adjustable-value" data-label="{{key}}" value="{{val}}">
                </label></td>
            </tr>
        {{/unless}}
    {{/each}}
        </tbody>
    </table>
</div>