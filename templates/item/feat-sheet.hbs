<form class="{{cssClass}} feat-sheet" autocomplete="off">
    <header class="sheet-header">
        <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}"/>
        <div class="header-fields">
          <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Name"/></h1>
            <div class="resource">
                <label class="resource-label">Type:</label>
                {{data.type}}
            </div>
            <div class="resource">
                <label class="resource-label">Categories:</label>
                {{#if data.data.categories}}
                    {{#each data.data.categories as |category id|}}{{#if id}}, {{/if}}{{category.value}}{{/each}}
                {{/if}}
                {{#unless data.data.categories}}
                    {{#each item.data._source.data.categories as |category id|}}{{#if id}}, {{/if}}{{category.value}}{{/each}}
                {{/unless}}
                {{log item data}}
            </div>
            {{#if data.data.isSupplied}}
            <div class="resource">
                <label class="resource-label">Source:</label> {{toTitleCase data.data.supplier.type}}: {{data.data.supplier.name}}
            </div>
            {{/if}}
        </div>
    </header>

    {{!-- Sheet Tab Navigation --}}
    <nav class="sheet-tabs tabs" data-group="primary">
        <a class="item" data-tab="description">Description</a>
        <a class="item" data-tab="attributes">Attributes</a>
    </nav>

    {{!-- Sheet Body --}}
    <section class="sheet-body">

        {{!-- Description Tab --}}
        <div class="tab" data-group="primary" data-tab="description">
            {{#if data.data.description}}
            {{editor content=data.data.description target="data.data.description" button=true owner=owner editable=editable}}
            {{/if}}
            {{#unless data.data.description}}{{{item.data._source.data.description}}}{{/unless}}
        </div>

        {{!-- Attributes Tab --}}
        <div class="tab attributes" data-group="primary" data-tab="attributes">
            {{> 'systems/swse/templates/item/parts/attributes.hbs'}}
<!--            <header class="attributes-header flexrow">-->
<!--                <span class="attribute-key">Attribute Key</span>-->
<!--                <span class="attribute-value">Value</span>-->
<!--            </header>-->

<!--            <ol class="attributes-list">-->
<!--            {{#each data.data.attributes as |attr key|}}-->
<!--                  <li class="attribute flexrow" data-attribute="{{key}}">-->
<!--                      <label class="attribute-key" type="text" name="data.attributes.{{key}}.key" value="{{key}}">{{attr.key}}</label>-->
<!--                      <input name="data.attributes.{{key}}.value" type="text" value="{{attr.value}}" placeholder="" title=""/>-->
<!--&lt;!&ndash;                      //<label class="attribute-value" type="text" name="data.attributes.{{key}}.value" value="{{attr.value}}" data-dtype="{{attr.dtype}}">{{attr.value}}</label>&ndash;&gt;-->
<!--                  </li>-->
<!--            {{/each}}-->
<!--            </ol>-->
        </div>
    </section>
</form>
